name: publish

on: [push]

jobs:
  publish-landsbooking-image:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Build the landsbooking Docker image
      run: |
        echo $CR_PAT | docker login ghcr.io -u jrobii --password-stdin
      env:
        CR_PAT: ${{ secrets.CR_PAT }}
